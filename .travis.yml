language: python
python:
  #  - "3.5"
  - "3.6"

install: true

script:
  - python -v -m unittest test/test.py      # test with checks
  - python -v -O -m unittest test/test.py   # test without checks
