language: python
python:
  #  - "3.5"
  - "3.6"

install: true

script:
  - python -m unittest test/test.py -v      # test with checks
  - python -O -m unittest test/test.py -v   # test without checks
