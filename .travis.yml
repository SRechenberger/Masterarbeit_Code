language: python
python:
  #  - "3.5"
  - "3.6"

install: true

script:
  - travis_wait python -m unittest test/test.py      # test with checks
  - travis_wait python -O -m unittest test/test.py   # test without checks
